<!--books/show-->
  <p>
    <strong>
      Title:
    </strong>
    <%= @book.title %>
  </p>
  <p>
    <strong>
      <%= @book.body %>
    </strong>
  </p>
  <%= link_to "Edit", edit_book_path(@books.id) %>
  |
  <%= link_to "Back", books_path %>